{
  "name": "pos-admin-app",
  "version": "1.0.0",
  "private": true,
  "description": "Platform admin app for tenant, package, order, and permission management",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch server.js --watch index.html --watch src --ext js,html,css,json,svg,png,jpg,jpeg,ico --exec \"node server.js\""
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
